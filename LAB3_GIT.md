# Git інструкції для Лабораторної роботи №3

## 📝 Файли для додавання до репозиторію

### Обов'язкові файли:

```bash
# Код проєкту
git add School.Infrastructure/
git add School.Console/Program.cs
git add School.Console/ProgramLab3.cs
git add School.Console/School.Console.csproj
git add School.sln

# База даних SQLite (згідно з завданням)
git add school.db

# Документація
git add LAB3_README.md
git add LAB3_INSTRUCTIONS.md
git add LAB3_ERD.md
git add LAB3_SUMMARY.md
git add LAB3_QUICKSTART.md

# Скрипти
git add build_lab3.cmd
git add run_lab3.cmd

# PDF документація (після створення)
git add LAB3_Documentation.pdf
```

### Файли, які НЕ потрібно додавати:

```
# Build результати
bin/
obj/
*.dll
*.pdb
*.exe

# IDE файли
.vs/
.vscode/
*.user
*.suo

# Тимчасові файли
*.tmp
*.log
```

## 🔧 Команди Git

### 1. Перевірити статус:
```bash
git status
```

### 2. Додати всі нові файли Lab3:
```bash
# Додати проєкт Infrastructure
git add School.Infrastructure/

# Додати оновлені файли Console
git add School.Console/Program.cs
git add School.Console/ProgramLab3.cs
git add School.Console/School.Console.csproj

# Додати оновлений solution
git add School.sln

# Додати базу даних SQLite
git add school.db

# Додати документацію
git add LAB3_*.md

# Додати скрипти
git add build_lab3.cmd run_lab3.cmd
```

### 3. Створити коміт:
```bash
git commit -m "Лабораторна робота №3: Entity Framework та Repository Pattern

- Створено проєкт School.Infrastructure
- Реалізовано моделі з зв'язками (1:1, 1:N, M:N)
- Використано Table-per-Type (TPT) наслідування
- Створено SchoolContext з Fluent API
- Реалізовано паттерн Repository
- Створено міграцію Entity Framework
- Застосовано до SQLite бази даних
- Оновлено CRUD сервіс для роботи з БД
- Оновлено консольний застосунок
- Додано повну документацію та ER-діаграму"
```

### 4. Відправити на GitHub:
```bash
git push origin main
```

## 📄 Додавання PDF документації

Після створення PDF:

```bash
# Додати PDF файл
git add LAB3_Documentation.pdf

# Додати скріншоти (якщо є окремою папкою)
git add screenshots/

# Створити коміт
git commit -m "Додано PDF документацію та скріншоти для Lab3"

# Відправити
git push origin main
```

## 📋 Checklist перед Push

- [ ] Скомпільовано проєкт без помилок
- [ ] Програма запускається без помилок
- [ ] База даних створюється та заповнюється даними
- [ ] Усі файли коду додано до Git
- [ ] База даних school.db додано до Git
- [ ] Документація додана (LAB3_*.md)
- [ ] PDF документація створена та додана
- [ ] ER-діаграма включена в документацію
- [ ] Скріншоти БД та виконання програми зроблені
- [ ] Коміт має зрозумілий опис
- [ ] Код відформатовано

## 🔍 Перевірка структури репозиторію

Після push, структура має бути:

```
repository/
├── School.Common/
│   ├── *.cs
│   └── School.Common.csproj
├── School.Console/
│   ├── Program.cs
│   ├── ProgramLab3.cs
│   └── School.Console.csproj
├── School.Infrastructure/          # Новий проєкт
│   ├── Data/
│   │   ├── SchoolContext.cs
│   │   └── SchoolContextFactory.cs
│   ├── Models/
│   │   ├── PersonModel.cs
│   │   ├── StudentModel.cs
│   │   ├── TeacherModel.cs
│   │   ├── CourseModel.cs
│   │   ├── GradeModel.cs
│   │   ├── StudentDetailsModel.cs
│   │   └── StudentCourseModel.cs
│   ├── Repositories/
│   │   ├── IRepository.cs
│   │   └── Repository.cs
│   ├── Services/
│   │   └── CrudServiceAsyncDb.cs
│   ├── Migrations/
│   │   └── 20251228_InitialCreate.cs
│   └── School.Infrastructure.csproj
├── School.Tests/
│   └── ...
├── School.sln
├── school.db                       # База даних SQLite
├── LAB3_README.md                  # Документація
├── LAB3_INSTRUCTIONS.md
├── LAB3_ERD.md
├── LAB3_SUMMARY.md
├── LAB3_QUICKSTART.md
├── LAB3_Documentation.pdf          # PDF звіт
├── build_lab3.cmd
└── run_lab3.cmd
```

## 🎯 Коміти для різних етапів

### Якщо робите окремі коміти:

```bash
# 1. Додати Infrastructure проєкт
git add School.Infrastructure/
git commit -m "Додано проєкт School.Infrastructure з моделями та DbContext"

# 2. Додати Repository
git add School.Infrastructure/Repositories/
git commit -m "Реалізовано паттерн Repository"

# 3. Додати міграції
git add School.Infrastructure/Migrations/
git commit -m "Додано початкову міграцію Entity Framework"

# 4. Оновити консоль
git add School.Console/
git commit -m "Оновлено консольний застосунок для роботи з БД"

# 5. Додати БД
git add school.db
git commit -m "Додано базу даних SQLite"

# 6. Додати документацію
git add LAB3_*.md
git commit -m "Додано документацію лабораторної роботи №3"

# 7. Додати PDF
git add LAB3_Documentation.pdf
git commit -m "Додано PDF звіт"
```

## ⚠️ Важливі нотатки

### SQLite база даних:
- За завданням, файл `school.db` **ПОТРІБНО** додати до репозиторію
- Це дозволяє перевіряючому побачити створену базу даних
- Переконайтеся, що база даних містить тестові дані

### .gitignore:
Переконайтеся, що `.gitignore` **НЕ** ігнорує:
- `*.db` файли (для lab3)
- `.md` файли документації
- `.pdf` файл звіту

### Розмір файлів:
- SQLite БД повинна бути невеликою (<1MB)
- PDF звіт має бути оптимізованим
- Скріншоти стискайте перед додаванням

## 📊 Приклад Pull Request опису

```markdown
# Лабораторна робота №3 - Entity Framework та Repository Pattern

## Виконані завдання:
- ✅ Створено проєкт School.Infrastructure
- ✅ Реалізовано моделі з зв'язками (1:1, 1:N, M:N)
- ✅ Використано Table-per-Type наслідування
- ✅ Створено SchoolContext з Fluent API
- ✅ Реалізовано паттерн Repository
- ✅ Створено міграцію та застосовано до SQLite
- ✅ Оновлено консольний застосунок
- ✅ Додано повну документацію та ER-діаграму

## Файли:
- Код: `School.Infrastructure/` (14 файлів)
- БД: `school.db` (7 таблиць)
- Документація: `LAB3_*.md` (5 файлів)
- PDF: `LAB3_Documentation.pdf`

## Як запустити:
```bash
dotnet run --project School.Console/School.Console.csproj
```

## Зв'язки:
- 1:1 - Student ↔ StudentDetails
- 1:N - Teacher → Courses, Course → Grades, Student → Grades
- M:N - Students ↔ Courses
- TPT - Person → Student/Teacher

Усі вимоги завдання виконані ✨
```

---

**Готово до відправки! 🚀**

